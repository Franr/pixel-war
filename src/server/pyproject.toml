[project]
name = "server"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "shared",
]

[tool.uv.sources]
shared = { path = "../shared", editable = true }

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
