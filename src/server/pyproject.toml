[project]
name = "server"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "shared",
]

[tool.uv.sources]
shared = { path = "../shared", editable = true }

[dependency-groups]
dev = [
    "ipdb>=0.13.13",
    "pytest>=9.1.1",
]
